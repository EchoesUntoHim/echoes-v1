# v1.15.29 상세 코드 비교 보고서 (Rule 5 준수)
작성일: 2026-04-30
작업자: Antigravity AI

---

## 작업 요약
버그 감사 결과 발견된 3가지 오류 중 우선순위 1, 2번을 수정하여 v1.15.29로 버전업.

---

## [수정 1] firestore.rules - meditation_history, generated_images 컬렉션 규칙 추가

### 원코드 (L116~L123)
```
    match /generated_lyrics/{lyricId} {
      allow list, get: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
      allow create: if isSignedIn() && incoming().userId == request.auth.uid;
      allow update: if isSignedIn() && (existing().userId == request.auth.uid || isAdmin()) && incoming().userId == existing().userId;
      allow delete: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
    }
  }
}
```

### 수정코드 (L116~L137)
```
    match /generated_lyrics/{lyricId} {
      allow list, get: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
      allow create: if isSignedIn() && incoming().userId == request.auth.uid;
      allow update: if isSignedIn() && (existing().userId == request.auth.uid || isAdmin()) && incoming().userId == existing().userId;
      allow delete: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
    }

    match /meditation_history/{docId} {
      allow list, get: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
      allow create: if isSignedIn() && incoming().userId == request.auth.uid;
      allow update: if isSignedIn() && (existing().userId == request.auth.uid || isAdmin()) && incoming().userId == existing().userId;
      allow delete: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
    }

    match /generated_images/{docId} {
      allow list, get: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
      allow create: if isSignedIn() && incoming().userId == request.auth.uid;
      allow update: if isSignedIn() && (existing().userId == request.auth.uid || isAdmin()) && incoming().userId == existing().userId;
      allow delete: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
    }
  }
}
```

**수정 이유:** 해당 컬렉션에 대한 규칙이 없어 Default Deny로 모든 접근이 차단되어 있었음.
              묵상 이미지 생성/저장/조회 전면 불가 상태였음.

---

## [수정 2] src/App.tsx - addToRenderQueue 연결

### 원코드 (L259~L269 구조분해)
```tsx
  const {
    audioBuffer, setAudioBuffer, uploadedAudio, setUploadedAudio, uploadedAudioName, setUploadedAudioName,
    isVideoRendering, isShortsGenerating, shortsHighlights, setShortsHighlights,
    mainVideoRef, tiktokVideoRef, shortsVideoRefs, handleAudioUpload, handleSingleImageUpload,
    generateImages, regenerateSpecificImage, regenerateShorts, startVideoRender,
    handleDownloadAll, downloadImageWithTitle, downloadBlogImage, renderedVideos, onRenderComplete,
    saveCurrentImagesToCloud
  } = useMediaLogic(...)
```

### 수정코드 (addToRenderQueue 추가)
```tsx
  const {
    ...
    saveCurrentImagesToCloud, addToRenderQueue
  } = useMediaLogic(...)
```

---

### 원코드 (L842~L855 MeditationTab)
```tsx
          {activeTab === 'meditation' && (
            <MeditationTab
              workflow={workflow}
              setWorkflow={setWorkflow}
              addLog={addLog}
              handleTabChange={handleTabChange}
              apiKey={apiKey}
              aiEngine={aiEngine}
              logs={logs}
              sunoTracks={sunoTracks}
              setSunoTracks={setSunoTracks}
              user={user}
            />
          )}
```

### 수정코드 (props 2개 추가)
```tsx
          {activeTab === 'meditation' && (
            <MeditationTab
              workflow={workflow}
              setWorkflow={setWorkflow}
              addLog={addLog}
              handleTabChange={handleTabChange}
              apiKey={apiKey}
              aiEngine={aiEngine}
              logs={logs}
              sunoTracks={sunoTracks}
              setSunoTracks={setSunoTracks}
              addToRenderQueue={addToRenderQueue}
              isRendering={isVideoRendering}
              user={user}
            />
          )}
```

**수정 이유:** addToRenderQueue, isRendering props가 미전달되어
              MeditationTab의 "대량 렌더링" 버튼이 항상 disabled 상태 또는 동작 불가였음.

---

## 검증 결과
- `npx tsc --noEmit` → ✅ 오류 0건
- TypeScript 컴파일 성공

---

## 미결 사항 (추후 패치 예정)
- [오류 3] React Key 중복 경고 → VideoTab.tsx, PublishTab.tsx, SettingsPage.tsx 추가 확인 필요
