# v1.15.25 상세 수정 보고서 (Detailed Diff)

## 1. 개요
가사 생성 시 슬래시(/) 사용 문제 해결, 분량 지침 정상화 및 MeditationTab 타입 오류 수정을 완료하였습니다.

## 2. 파일별 수정 내역

### 가. src/prompts/Lyrics.txt
```diff
-# [General Formatting Rules - APPLY TO ALL PERSONAS]
-- SECTION TAGS: Put in square brackets on their OWN line in BOTH fields. Add a blank line after each tag.
-- 1:1 SYNC: Korean and English must have identical line counts.
-- VOLUME (3-4 MIN): Generate at least 32-40 lines total.
-- NO BOLDING: NEVER use **.
+# [General Formatting Rules - APPLY TO ALL PERSONAS]
+- SECTION TAGS: Put in square brackets on their OWN line in BOTH fields. Add a blank line after each tag.
+- 1:1 SYNC: Korean and English must have identical line counts and matching meanings line by line.
+- VOLUME (3-4 MIN): Generate a substantial volume of lyrics enough for a full 3-4 minute song duration. 
+- NO SLASHES: NEVER use slashes (/) for line breaks. Use ACTUAL newlines (\n) to separate every single line.
+- NO BOLDING: NEVER use **.
```

### 나. src/hooks/useLyricsLogic.ts
```diff
-        - SONG VOLUME (4+ MIN): Generate a substantial volume of lyrics (multiple Verses, repeated Choruses, Bridge, Outro) to ensure a 4+ minute duration.
+        - SONG VOLUME (3-4 MIN): Generate a substantial volume of lyrics (multiple Verses, repeated Choruses, Bridge, Outro) to ensure a 3-4 minute duration.
+        - NO SLASHES: Use ACTUAL newlines (\n) for every line. NEVER use slashes (/) for line breaks.
         
         [TITLE FORMAT - MANDATORY]
-        [TargetTag][Korean Title]_[English Title]
+        [분류]한글_영어 (예: [CCM]나의 기도_My Prayer)
```

### 다. src/components/MeditationTab.tsx
```diff
-      const { prompt, symbol, position, dayInfo: buildDayInfo } = buildMeditationPrompt(index, item.verse);
+      const { prompt, position } = buildMeditationPrompt(index, item.verse);
...
-              symbol: symbol,
...
-                                <span className="text-[9px] font-normal text-gray-500">{imageMeta?.color} | {imageMeta?.symbol}</span>
+                                <span className="text-[9px] font-normal text-gray-500">{imageMeta?.color}</span>
```

## 3. 검증 결과
- `MeditationTab.tsx` 타입 에러 해결 완료.
- AI 가사 생성 시 줄바꿈 및 제목 형식 지침 반영 완료.
- 15자 미만 제약 해제로 인한 가사 품질 향상 기대.

---
**보고자: Antigravity**
