# [v1.12.23] v3 가사 프롬프트 유지 및 음악 엔진/편곡 기능 삭제 플랜 (수정본)

본 계획서는 v2 계획서의 중대한 오류(Suno 프롬프트 삭제)를 바로잡고, 사용자님의 의도에 맞게 '음악 엔진'과 '편곡/분리 기능'만을 정밀 타격하여 제거하기 위한 최종 수정 플랜입니다.

## 1. 지시사항 재확인 (Corrected Direction)
- **프롬프트 유지 (CRITICAL)**: 가사 생성 시 수반되는 Suno AI 프롬프트 생성 로직 및 UI는 그대로 유지합니다.
- **음악 엔진 삭제**: 설정 페이지의 음악 엔진 선택 UI와 하드코딩된 관련 로직만 삭제합니다.
- **편곡/분리 기능 삭제**: `ArrangementWorkspace`, `MusicGenerator` 컴포넌트와 관련 탭을 삭제합니다.
- **하드코딩 제거**: 버전 표기(`v1.12.23 PREMIUM`) 및 렌더링 URL(`RENDER_API_URL`) 상수를 통한 일원화는 유지합니다.

## 2. 세부 수정 계획

### [A] UI 및 컴포넌트 정리
- **src/App.tsx**: 
    - 사이드바에서 '편곡', '음원 분리' 탭 제거. ('가사 & 프롬프트' 라벨 및 기능 유지)
    - `renderContent`에서 편곡 및 분리 탭 렌더링 로직 삭제.
    - 버전 표시를 상수 기반 `v1.12.23 PREMIUM`으로 통일.
- **src/components/LyricsTab.tsx**:
    - **가사 및 프롬프트 관련 모든 UI 유지** (입력창, 재생성 버튼 등).
    - 하드코딩된 '음악 엔진' 관련 텍스트 표시만 제거하거나 적절히 수정.
- **src/components/SettingsPage.tsx**:
    - 음악 엔진 선택 섹션 제거.

### [B] 기능 및 로직 정규화
- **src/App.tsx**:
    - `generateLyrics`, `generatePromptOnly` 함수 **유지** 및 로직 보존.
    - 하드코딩된 모델명을 상태 변수(`aiEngine`, `imageEngine`)로 교체.
- **src/constants.ts**:
    - 중앙 관리 상수(`DEFAULT_AI_ENGINE`, `DEFAULT_IMAGE_ENGINE`, `RENDER_API_URL`) 확립.

### [C] 파일 삭제 (Approval Required)
- `src/components/ArrangementWorkspace.tsx` [DELETE]
- `src/components/MusicGenerator.tsx` [DELETE]

## 3. 검증 계획
1. **가사 & 프롬프트 확인**: 가사 생성 시 Suno 프롬프트가 예전처럼 정상적으로 생성되는지 확인.
2. **사이드바 확인**: 편곡/분리 메뉴가 사라졌는지 확인.
3. **설정 확인**: 음악 엔진 설정 메뉴가 제거되었는지 확인.

---
**사용자님의 "승인"을 기다립니다. 승인 전까지는 코드를 절대 수정하지 않겠습니다.**
