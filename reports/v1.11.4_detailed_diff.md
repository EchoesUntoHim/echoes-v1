# 버전 v1.11.4 상세 변경 내역 (Detailed Diff)

## 📌 변경 개요
- **작업일**: 2026-04-25
- **버전**: v1.11.4
- **목적**: 앱 구글 로그인과 유튜브/블로그 플랫폼 연동 인증 로직의 완전한 분리
- **관련 파일**: `src/firebase.ts`, `src/App.tsx`, `package.json`, `src/components/LandingPage.tsx`

---

## 💻 코드 변경 상세 (Diff)

### 1. `src/firebase.ts`
**[변경 전]**
```typescript
// 기본 로그인용 공급자 (추가 권한 없음)
export const googleProvider = new GoogleAuthProvider();
```

**[변경 후]**
```typescript
// 기본 로그인용 공급자 (추가 권한 없음 - 앱 접속 전용)
export const googleProvider = new GoogleAuthProvider();

// 플랫폼 연동용 공급자 (유튜브 업로드 및 블로거 권한 포함)
export const youtubeProvider = new GoogleAuthProvider();
youtubeProvider.addScope('https://www.googleapis.com/auth/youtube.upload');
youtubeProvider.addScope('https://www.googleapis.com/auth/blogger');
```

**[변경 전 (하단)]**
```typescript
export const signInWithGoogle = async () => { ... }
export const logout = () => signOut(auth);
```

**[변경 후 (하단)]**
```typescript
export const signInWithGoogle = async () => { ... }

// Platform Connection Helper (신설)
export const signInForYouTube = async () => {
  try {
    const result = await signInWithPopup(auth, youtubeProvider);
    const credential = GoogleAuthProvider.credentialFromResult(result);
    const accessToken = credential?.accessToken;
    return { user: result.user, accessToken };
  } catch (error: any) {
    console.error("Error signing in with YouTube provider:", error);
    const errorMessage = error.code ? `[${error.code}] ${error.message}` : String(error);
    throw new Error(errorMessage);
  }
};

export const logout = () => signOut(auth);
```

### 2. `src/App.tsx`
**[상단 임포트]**
- `signInWithGoogle` 외에 `signInForYouTube` 추가 임포트 완료.

**[플랫폼 연동 팝업: `handlePlatformLoginConfirm`]**
**[변경 전]**
```typescript
          } else {
            const result = await signInWithGoogle();
            setUser(result.user);
            if (result.accessToken) {
```
**[변경 후]**
```typescript
          } else {
            const result = await signInForYouTube(); // 연동 전용 제공자로 교체
            if (result.accessToken) { // setUser(result.user) 삭제 (앱 로그인과 완전 분리)
```

**[유튜브 업로드 버튼: `handlePlatformUpload`]**
**[변경 전]**
```typescript
        if (!currentToken) {
          addLog("🔑 유튜브 연동이 필요합니다. 구글 로그인을 진행해주세요.");
          try {
            const result = await signInWithGoogle();
            setUser(result.user);
```
**[변경 후]**
```typescript
        if (!currentToken) {
          addLog("🔑 유튜브 연동이 필요합니다. 연동을 진행해주세요.");
          try {
            const result = await signInForYouTube(); // 연동 전용 제공자로 교체
            // setUser(result.user) 삭제 (앱 로그인과 완전 분리)
```

*(참고: 3265번 줄에 위치한 사이드바의 **[구글 로그인]** 버튼은 기존대로 순수한 `signInWithGoogle`을 사용하도록 유지하여, 연동 권한 없이 안전하게 로그인만 수행하도록 완벽 분리 완료됨.)*

### 3. 버전 업그레이드
- `package.json`: `"version": "1.10.0"` ➔ `"1.11.4"`
- `src/components/LandingPage.tsx`: `v1.9.2` ➔ `v1.11.4`

## ✅ 결과 및 기대 효과
- **빌드 테스트 완료**: `npm run build` 오류 없이 정상 통과.
- 앱 로그인은 추가 권한 요구 없이 가볍게 진행되며, 연동 버튼을 누를 때만 유튜브/블로그 전용 팝업이 뜨게 됨.
- 향후 "회원 등급제" 및 "결제/악보 판매 시스템"을 위한 완벽하게 분리된 권한 통제 기반 마련 완료.
