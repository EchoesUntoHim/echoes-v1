# [v1.12.5] 시스템 전체 롤백 및 안정화 계획서

## 1. 개요
- 오늘 작업 중 발생한 불안정성(백지 화면, 애니메이션 오류 등)을 해결하기 위해, 검증된 최후의 안정 버전인 **v1.12.5**로 시스템 전체를 롤백함.

## 2. 규칙 준수 확인
- **규칙 1**: 롤백 시 백업본을 사용함.
- **규칙 3-2**: '승인' 확인 후 작업을 진행함.
- **규칙 7**: 모든 보고는 한글로 작성함.

## 3. 상세 롤백 절차

### 3.1 백업 파일 검증
- `backups/1.12.5.zip` (2026-04-27 01:09 생성) 파일을 임시 디렉토리에 압축 해제하여 내용 검토.
- 버전이 `v1.12.5`가 맞는지, 오늘 추가된 `useRef`, `useCallback` 등이 없는지 확인.
- 만약 위 파일이 부적합할 경우, `backups/20260426_165747_v1.12.1.zip`을 차선책으로 사용.

### 3.2 클린 복구 (Clean Restore)
- 현재 작업 중인 `src`, `rules`, `reports` 폴더를 완전히 삭제 (충돌 방지).
- 검증된 백업 압축 파일을 현재 루트 디렉토리에 해제.

### 3.3 상태 동기화
- `App.tsx` 버전 표기 확인: `v1.12.5 PREMIUM`.
- `APP_CODE_MAP.md` 복구 확인.
- 로컬 스토리지 데이터 등 환경 설정 체크.

## 4. 확인 절차
- 앱이 정상적으로 실행되는지(백지 화면 해결) 확인.
- 가사 싱크 및 기존 기능들이 어제와 동일하게 작동하는지 확인.

---
**위 롤백 계획에 대해 '승인'을 요청드립니다.**
