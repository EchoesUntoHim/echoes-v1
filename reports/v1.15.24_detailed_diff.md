# ImageTab Restoration Detailed Diff (v1.15.24)

## 1. 하단 업로드 바 동적 생성 로직
```diff
- <label className="..."><span className="...">쇼츠1</span>...</label>
- <label className="..."><span className="...">쇼츠2</span>...</label>
- <label className="..."><span className="...">쇼츠3</span>...</label>
+ {Array.from({ length: shortsCount }).map((_, i) => (
+   <label key={i} className="...">
+     <Upload className="..." />
+     <span className="...">쇼츠 {i + 1}</span>
+     <input type="file" className="hidden" onChange={(e) => handleSingleImageUpload(`숏츠${i + 1}`, e)} />
+   </label>
+ ))}
```

## 2. 다운로드 확인 팝업 로직 추가
```diff
+ const handleDownloadClick = (img: any) => {
+   const withDesign = window.confirm("디자인된 타이틀을 포함하여 다운로드하시겠습니까?\n\n'확인'을 누르면 디자인 포함, '취소'를 누르면 원본 이미지만 다운로드됩니다.");
+   downloadImageWithTitle(img, withDesign);
+ };
...
- <button onClick={() => downloadImageWithTitle(img)} ...>
+ <button onClick={() => handleDownloadClick(img)} ...>
```

## 3. 타이틀 컨트롤 패널(VideoSettingsPanel) 전체 이미지 적용
```diff
- {typeKey === 'main' && (
-   <VideoSettingsPanel ... />
- )}
+ <VideoSettingsPanel
+   type={img.label}
+   settings={settings}
+   onChange={(newSettings) => setWorkflow(prev => ({ ...prev, imageSettings: { ...prev.imageSettings, [typeKey === 'shorts' ? 'shorts' : typeKey]: newSettings } }))}
+   showLyricsControls={false}
+ />
```

## 4. 드롭다운(Select) 메뉴 스타일 고정
```diff
- <select className="w-full bg-white/5 ...">
+ <select className="w-full bg-[#1A1F26] border border-white/10 rounded-xl px-4 py-3 text-xs font-bold text-white ...">
+   {ART_STYLES.map(v => <option key={v} value={v} className="bg-[#1A1F26]">{v}</option>)}
```

## 5. 숏츠 개수 조절 바 시각적 보정
```diff
- <div className="... w-[60%] ..." />
+ <div 
+   className="absolute top-0 left-0 h-full bg-primary rounded-full transition-all duration-300 ..." 
+   style={{ width: `${(shortsCount / 5) * 100}%` }}
+ />
```
