# v1.5.7-patch6 가사 싱크 최종 해결 계획 (스타일 유지)

## 1. 개요
현재의 2+1 대조 레이아웃을 유지하면서, 비디오 플레이어에 싱크가 반영되지 않던 로직 오류를 해결합니다.

## 2. 주요 수정 사항

### [VideoTab.tsx]
*   **싱크 우선순위 교정**: `VideoPlayer`에 전달되던 `timedLyrics` 프롭스를 제거합니다. 
*   **효과**: 플레이어가 사용자가 입력한 '원곡 가사'가 매핑된 텍스트(`finalVideoLyrics`)를 소스로 사용하여, 분석된 시간 정보에 맞춰 정확한 자막을 출력합니다.

## 3. 상세 수정 파일
*   **[MODIFY] [VideoTab.tsx](file:///c:/Users/sando/Desktop/ccmpro/src/components/VideoTab.tsx)**: `VideoPlayer` 호출부에서 `timedLyrics` 제거.

## 4. 검증 계획
*   원곡 가사 입력 시 비디오 플레이어의 자막이 분석된 싱크에 맞춰 즉시 변경되는지 확인합니다.
