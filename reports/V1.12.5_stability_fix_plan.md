# v1.12.5 Stability Fix Plan (Report)

## 🎯 Goal
Restore missing UI components and optimize API costs by preventing redundant translations on page refresh.

## 📌 Proposed Changes

### [Component] src/App.tsx

#### [MODIFY] [App.tsx](file:///c:/Users/sando/Desktop/ccmpro/src/App.tsx)
- **Translation Guard (L845)**: Added logic to initialize `lastTranslatedLyricsRef` on the first load without triggering a translation.
- **LyricsTab Connectivity (L3364)**: Passed `sunoTracks` and `setSunoTracks` to the `LyricsTab` component to enable the history list.

## ✅ Verification Plan

### Manual Verification
1. **Refresh the page**: Confirm that the log "자동 번역을 수행합니다" does NOT appear immediately after refresh.
2. **Lyrics Tab**: Navigate to the Lyrics tab and verify that the "Suno Tracks History" list is visible and functional.
3. **Edit Lyrics**: Manually edit a line in the lyrics and confirm that the auto-translation triggers after the 10-second delay.

## 📂 Backup Plan
- Upon user approval and verification, perform a full backup to `backups/1.12.5.zip`.
