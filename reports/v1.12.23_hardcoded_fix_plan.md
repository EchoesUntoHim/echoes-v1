# v1.12.23 하드코딩 모델명 긴급 제거 및 최종 완성 계획서

`App.tsx` 내부에 하드코딩되어 있던 잘못된 모델명을 제거하고, 설정된 엔진이 정상적으로 호출되도록 로직을 보정합니다.

## 점검 결과 보고 (Post-Audit Report)

- **원인 판명**: `App.tsx` 내의 이미지 재생성 로직(L2867)에 `gemini-3.1-flash-image-preview`가 상수가 아닌 하드코딩으로 박혀 있어, 드롭다운 설정과 무관하게 실패가 발생함.
- **해결 방안**: 하드코딩된 문자열을 삭제하고 상태 변수(`imageEngine`)를 참조하도록 수정합니다.

## User Review Required

> [!IMPORTANT]
> - **동적 모델 할당**: 이제 설정 페이지에서 선택한 모델이 모든 이미지 생성 로직에 정확히 반영됩니다.
> - **기본값 최신화**: `useState` 초기값을 현재 가성비 모델인 `imagen-4.0-fast`로 설정합니다.

## Proposed Changes

### [App Logic Settings]

#### [MODIFY] [App.tsx](file:///c:/Users/sando/Desktop/ccmpro/src/App.tsx)
- L213: `useState` 초기값 수정 (`imagen-4.0-fast`)
- L2867: 하드코딩 모델명 삭제 및 `imageEngine` 변수 적용.

## Verification Plan

### Manual Verification
- **동작 확인**: 사용자님이 개별 이미지 재생성 시도 시, 더 이상 404 에러 없이 설정된 모델로 호출되는지 확인.
