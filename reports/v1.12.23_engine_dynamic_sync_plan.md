# v1.12.23 엔진 동적 변수화 및 하드코딩 제거 계획서

프로젝트 내의 모든 AI 엔진 호출부를 동적 변수(`imageEngine`, `aiEngine` 등)로 전환하여, 설정 페이지의 선택 사항이 100% 반영되도록 보정합니다.

## 점검 결과 보고 (Post-Audit Report)

- **하드코딩 발견**: `App.tsx` (L213, L2867) 및 `ArrangementWorkspace.tsx` 등에서 구식 모델명이 직접 입력되어 있음을 확인.
- **해결 방안**: 모든 직접 입력된 모델명을 삭제하고, 사용자 인터페이스(UI)에서 선택된 상태 변수를 참조하도록 전면 수정합니다.

## User Review Required

> [!IMPORTANT]
> - **설정값 100% 반영**: 이제 어떤 페이지에서 엔진을 변경하든, 실제 API 호출 시 해당 엔진이 즉시 적용됩니다.
> - **코드맵 동기화**: 엔진 호출부의 위치를 코드맵에 기록하여 향후 관리를 용이하게 합니다.

## Proposed Changes

### [Core Logic Cleanup]

#### [MODIFY] [App.tsx](file:///c:/Users/sando/Desktop/ccmpro/src/App.tsx)
- L213: `imageEngine` 기본값 수정.
- L2867: 이미지 재생성 로직의 `model`을 `imageEngine`으로 변경.
- 기타 `aiEngine` 관련 폴백 값 정돈.

#### [MODIFY] [ArrangementWorkspace.tsx](file:///c:/Users/sando/Desktop/ccmpro/src/components/ArrangementWorkspace.tsx)
- 하드코딩된 폴백 모델명 수정.

### [Documentation Update]

#### [MODIFY] [APP_CODE_MAP.md](file:///c:/Users/sando/Desktop/ccmpro/rules/APP_CODE_MAP.md)
- **[엔진 호출 루트]** 섹션 신설 및 핵심 라인 기록.

## Verification Plan

### Manual Verification
- **드롭다운 변경 테스트**: 설정에서 이미지 엔진을 변경한 후, 실제 이미지 생성 시 네트워크 탭이나 로그에서 변경된 엔진이 호출되는지 확인.
