# 작업 계획서 (v1.15.5_stabilization_plan) - 최종 확인본

## 1. 개요
사용자의 디자인 스타일과 프롬프트 설성(이미지/텍스트 스타일)을 100% 보존하면서, 기술적인 동작 오류만 해결하기 위한 최종 안정화 계획입니다.

## 2. 엄격 준수 사항 (STRICT PROHIBITION)
- **묵상 프롬프트 수정 금지**: `MeditationTab.tsx` 내의 이미지 생성 프롬프트 및 텍스트 생성 프롬프트 문구는 절대 건드리지 않음.
- **디자인 스타일 유지**: 기존 UI 레이아웃, 컬러, 텍스트 스타일(CSS/Tailwind) 등 시각적 요소 수정 금지.

## 3. 기능적 수정 대상 (Bug Fix ONLY)
### 3.1 자막 동기화 (Subtitle Sync)
- **목적**: 간주/후주 구간 자막 잔상 제거.
- **수정**: `App.tsx` 및 `useLyricsLogic.ts`의 오디오 분석 프롬프트에 [STRICT TIMING] 규칙(빈 타임스탬프)만 추가.

### 3.2 묵상 단일 이미지 생성 로직
- **목적**: 특정 날짜 이미지 재생성 기능 활성화.
- **수정**: `MeditationTab.tsx`에 `handleGenerateSingleImage` 함수 구현 및 기존 UI에 기능만 매핑. (스타일 수정 없음)

### 3.3 SDK 및 설정 오류
- **목적**: 이미지 재생성 시 에러(`generateContent` -> `generateImages`) 해결.
- **수정**: `App.tsx` 및 `constants.ts`의 관련 메서드 및 설정값 수정.

## 4. 상세 수정 위치
1. **`src/App.tsx`**: 가사 분석 프롬프트 보강, SDK 메서드 수정, 버전 통합(`v1.15.5`).
2. **`src/hooks/useLyricsLogic.ts`**: 가사 분석 프롬프트 동기화.
3. **`src/components/MeditationTab.tsx`**: `handleGenerateSingleImage` 기능 추가 (기존 스타일 보존).
4. **`src/constants.ts`**: Imagen 4.0 Pro 엔진 옵션 추가.

---
**작성자**: Antigravity AI
**상태**: **최종 승인 대기 중**
